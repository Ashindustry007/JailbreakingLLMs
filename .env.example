# Copy this file to `.env` and fill in your real keys.
# `.env` is gitignored and must NEVER be committed.
#
#   cp .env.example .env
#   # edit .env, then load it:
#   set -a && source .env && set +a

# Together.ai — attacker (Mixtral) + Llama Guard judge
TOGETHER_API_KEY=

# OpenAI — gpt-4o target
OPENAI_API_KEY=

# Avoid wandb login prompt blocking the run
WANDB_MODE=offline
